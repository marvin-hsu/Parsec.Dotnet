# Description

<!-- What does this PR change, and why? -->

## Checklist

- [ ] Commits are signed off (`git commit -s`, DCO)
- [ ] `dotnet build` passes with zero warnings
- [ ] `dotnet test` passes; new behaviour is covered by tests
- [ ] `dotnet format --verify-no-changes` passes
- [ ] Public API changes are documented (XML docs + CHANGELOG entry)
- [ ] No new dependencies, or maintainer approval obtained
- [ ] Security-sensitive changes include a rationale below

## Security considerations

<!-- Does this touch key material, IPC transport, parsing of untrusted input? If yes, explain. If no, write "None". -->
